[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (for client-side routing). Safe even without a router.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

