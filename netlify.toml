[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
# SPA fallback (safe even without a router).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
